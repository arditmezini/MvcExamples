@model MvcExamples.Models.Visitor.VisitorViewModel

@{
    ViewBag.Title = "Index";
}

<div class="container">
    <div class="row">
        @{Html.RenderPartial("_VisitorSearchForm", Model.VisitorSearchForm);}
    </div>
    <div class="row">
        @{Html.RenderPartial("_VisitorList", Model.VisitorList);}
    </div>
</div>

